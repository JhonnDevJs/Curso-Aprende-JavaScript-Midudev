

suma(1, 2)

function suma(a, b) { //esto es una funcion normal
  return a + b
}

sum(1, 3)

const sum = function (a, b) { //esto se conoce como una funcion anonima o de expresion
  return a + b
}

//Por que no funciona? es debido a algo llamado hoisting que hace que una funcion de expresion no puede llamarce antes de que se declare