// Funciones flecha

const suma = (a, b) => {
  return a + b
}

// cuando nuestra funcion es lo suficientemente sencilla podemos hacer uso del return implicito simplificando la funcion de tipo flecha asi:

const sumar = (a, b) => a + b