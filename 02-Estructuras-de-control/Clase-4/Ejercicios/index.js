//  Expresiones y declaraciones

// Esto es una declaración por que estamos declarando que tenemos una variable con un valor

let variableDelcarada = 100

// Esto es una expresión por que estamos creando una evaliación que nos retuna un resultato (valor) = False

10 >= 10 && 'a' === 'b' // = false